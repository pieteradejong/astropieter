---
title: 'Test fixture: MDX and LaTeX'
description: 'Rendering fixture for tests/content.spec.ts. A draft, so it never ships.'
pubDate: 'Jan 01 2020'
tags: ['test']
draft: true
---

Fixture for `tests/content.spec.ts`: math must render in `.mdx` posts too.

Inline math: $a^2 + b^2 = c^2$ sits in the sentence.

$$
\frac{1}{1 + e^{-x}}
$$
